# ── Base de Datos ──
# DATABASE_URL=postgresql://postgres:RutA1_G3o_2026!%23@localhost:5432/rutaigeoproxi_db
DATABASE_URL=sqlite:///./rutaigeoproxi.db

# ── JWT ──
SECRET_KEY=cambiar_esta_llave_en_produccion
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=120

# ── Admin Seed ──
ADMIN_EMAIL=admin@rutaigeoproxi.com
ADMIN_PASSWORD=Admin2026!#

# ── Debug (solo desarrollo) ──
DEBUG_RESET_TOKEN=true

# ── CORS ──
CORS_ORIGINS=http://localhost:3000,http://localhost:4200,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:4200,http://127.0.0.1:5173

# ── Firebase Storage (opcional — dejar vacío para usar disco local) ──
FIREBASE_CREDENTIALS_PATH=
FIREBASE_STORAGE_BUCKET=

# ── IA — Clasificación de Incidentes ──
YOLO_MODEL_PATH=yolov8n.pt
WHISPER_MODEL_SIZE=base

# ── SMTP (Brevo) ──
SMTP_SERVER=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=a8732b001@smtp-brevo.com
SMTP_PASSWORD=escribe_tu_clave_smtp_aqui
FROM_EMAIL=no-reply@rutaigeoproxi.com